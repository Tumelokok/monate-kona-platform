# Monate Kona — Next.js Starter

Scaffolded minimal Next.js 15 + TypeScript + Tailwind starter for the Monate Kona website.

Commands:

```bash
npm install
npm run dev
```

Next steps:
- Add logo files to `public/` and update `components/Navbar.tsx` and `components/Footer.tsx`.
- Implement pages and components following the project plan.
